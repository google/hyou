[tox]
envlist = py27, py33, py34, py35, py36, lint

[testenv]
deps =
    -r{toxinidir}/requirements.txt
    -r{toxinidir}/requirements_dev.txt
commands =
    nosetests -v --nologcapture {posargs}

[testenv:lint]
commands =
    flake8 hyou test setup.py
